# Copy to android/keystore.properties and fill in.
# That file is git-ignored — never commit real credentials.
storeFile=medication-identifier-release.keystore
storePassword=
keyAlias=medication-key
keyPassword=
